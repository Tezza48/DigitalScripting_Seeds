#pragma strict

public class Game extends MonoBehaviour {

      public var generator : MazeGenerator;
      public var parser : MazeParser;
      public var width : int;
      public var height : int;
      public var tileSize : int;

      function Start () {
            
      }
}
