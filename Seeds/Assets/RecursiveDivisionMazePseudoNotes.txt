width
height
seed

random.seed = seed

mazeGrid = new bool[width, height]

// make constants for describing directions

HORIZONTAL = 1
VERTICAL = 2

ChooseDirection (width, height)
      if width < height
            return HORIZONTAL
      else if height < width
            return VERTICAL
      else
            // Randomly Pick

Divide (MazeGrid, x, y, width, height, orientation)

      // this method is recursive
      // return if width < 2 or height < 2

      bool isHorizontal = (orientation == HORIZONTAL)


      if isHorizontal
            // find a position to draw the wall
            wallX = x
            wallY = y + rand(height - 2)

            // find a position to draw the passage
            passageX = wallX + rand(width)
            passageY = wallY

            // find the length to make the wall
            length = width

      else
            wallX = x + rand(width - 2)
            wallY = y

            passageX = wallX
            passageY = wallY + rand(height)

            length = height

            direction = east

      for i < length
            if isHorizontal
                  grid [wallX + i, wallY] = true
            else
                  grid [wallX, wallY + i] = true

      grid[passageX, passageY] = false
